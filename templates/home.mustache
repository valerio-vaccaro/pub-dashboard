<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="utf-8">
      <meta name="description" content="Liquid.beer">
      <meta name="author" content="Drunkoshi Franziskanoto">
      <link rel="icon" href="">
      <title>Pub</title>
      <link href="/static/bootstrap.min.css" rel="stylesheet">
      <style type="text/css" style="display: none !important;">
         div#sl{
         width: 80%;
         margin: 0 auto;
         }
      </style>
   </head>
   <body>
      <nav class="navbar navbar-inverse navbar-fixed-top">
         <div class="container">
           <a class="navbar-brand navbar-right" href="/logout">Log out</a>
           <div class="navbar-header">
             <a class="navbar-brand" href="/">Home</a>
             <a class="navbar-brand" href="/lbtc">LBTC</a>
             <a class="navbar-brand" href="/asset">Asset</a>
             <a class="navbar-brand" href="/about">Credits</a>
           </div>
         </div>
      </nav>
      <div class="container-fluid">
         <br/><br/><br/><br/>
         <div id="sl">
            <br/><br/>
            <div class="well">

               <h3>Pub board</h3>
               <p>Board: {{uname}}</p>
               <p>Uptime: {{uptime}}</p>
               {{#admin}}
               <div class="form-group row">
                  <form action="/" method="post">
                  <input type="hidden" id="command" name="command" value="linux">
                  <div class="col-sm-10">
                     <button type="submit" class="btn btn-danger" name="action" value="stop">Stop</button>
                  </div>
                  </form>
               </div>
               {{/admin}}

               <h3>Liquid Status</h3>
               <p>{{info.headers}} headers and {{info.blocks}} downloaded blocks.</p>
               {{#admin}}
               <p><span class="badge badge-primary">{{liquidStatus}}</span> with {{info.headers}} headers and {{info.blocks}} downloaded blocks.</p>
               <div class="form-group row">
                  <form action="/" method="post">
                  <input type="hidden" id="command" name="command" value="liquid">
                  <div class="col-sm-10">
                     <button type="submit" class="btn btn-success" name="action" value="start">Start</button>
                     <button type="submit" class="btn btn-warning" name="action" value="restart">Restart</button>
                     <button type="submit" class="btn btn-danger" name="action" value="stop">Stop</button>
                  </div>
                  </form>
               </div>
               {{/admin}}

               <h3>TOR</h3>
               <p>Address {{torAddr}}</p>
               {{#admin}}
               <p><span class="badge badge-primary">{{torStatus}}</span> with address {{torAddr}}</p>
               <div class="form-group row">
                  <form action="/" method="post">
                  <input type="hidden" id="command" name="command" value="tor">
                  <div class="col-sm-10">
                     <button type="submit" class="btn btn-success" name="action" value="start">Start</button>
                     <button type="submit" class="btn btn-warning" name="action" value="restart">Restart</button>
                     <button type="submit" class="btn btn-danger" name="action" value="stop">Stop</button>
                     <button type="submit" class="btn btn-info" name="action" value="change">Change address</button>
                  </div>
                  </form>
               </div>
               {{/admin}}

               {{result}}

            </div>
         </div>
      </div>
      </div>
      <script src="/static/jquery.min.js"></script>
      <script src="/static/bootstrap.min.js"></script>
   </body>
</html>
